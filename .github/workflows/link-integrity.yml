name: Link Integrity

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  check:
    uses: ZenCloudAU/velocity-architecture/.github/workflows/link-integrity.yml@main
    with:
      site_url: 'https://zencloudau.github.io/vsf-match/'
      repo_name: 'vsf-match'
